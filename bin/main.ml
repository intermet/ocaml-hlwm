(* open! Hlwm.Hc *)
(* open! Hlwm.Hc.Helpers *)

(* let buffer = Buffer.create 50 *)

(* let cmd = foreach_client (echo_client_field "class") *)

(* let () = eval buffer Nil cmd *)
(* let () = Stdio.print_endline (Buffer.contents buffer) *)
